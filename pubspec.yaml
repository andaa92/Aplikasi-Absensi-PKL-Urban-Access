name: absensi_pkl_urban
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.2.0
  http: ^1.2.0
  shared_preferences: ^2.5.2
  device_info_plus: ^10.1.0
  local_auth: ^2.3.0
  encrypt: ^5.0.1
  crypto: ^3.0.2
  cupertino_icons: ^1.0.8
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/medima.jpeg
    - assets/urbanaccess.jpeg
